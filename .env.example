# === P4 Quality Repair Tool – Environment Variables ===
# Copy to .env and fill in real values:  cp .env.example .env

# Data directory for session storage
DATA_DIR=data

# ── LLM Model Selection ────────────────────────────────────────
# Default model when no explicit choice is made
LLM_DEFAULT_MODEL=gpt-4o-mini

# Task-based routing (auto mode):
#   HIGH/CRITICAL findings → strong model
#   MEDIUM/LOW findings    → fast model
LLM_STRONG_MODEL=gpt-5-mini
LLM_FAST_MODEL=gpt-4o-mini

# ── OpenAI ──────────────────────────────────────────────────────
# Get key at: https://platform.openai.com/api-keys
#OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# ── Anthropic ───────────────────────────────────────────────────
# Get key at: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-3-5-haiku-20241022

# ── Ollama (local, free) ────────────────────────────────────────
# Install: https://ollama.com  then: ollama pull llama3.1:8b
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.1:8b

# ── Shared LLM Settings ────────────────────────────────────────
TOKEN_BUDGET=20000
MAX_REPAIR_ISSUES=10

# Storage backend (local only for PoC)
STORAGE_BACKEND=local

# Log level
LOG_LEVEL=INFO
